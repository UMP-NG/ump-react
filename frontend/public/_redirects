# sitemap.xml/robots.txt are generated by the backend (they need live DB
# data), not shipped as static files here — proxy them to the API host so
# they're reachable at the site's own domain, matching what Google expects.
/sitemap.xml  https://ump-backend.onrender.com/sitemap.xml  200
/robots.txt   https://ump-backend.onrender.com/robots.txt   200

/* /index.html 200
